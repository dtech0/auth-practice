## Troubleshooting

### Issue: Express Server Starts but Application Does Not Work

While running the Express server, the terminal showed:

```bash
Server is running on http://localhost:3000

### Solution :Change the Port Number


PORT=8080 
pnpm start